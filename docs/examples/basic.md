# Basic Example

Source: `examples/basic.cpp`

```bash
cmake -S . -B build
cmake --build build --target unillm_example
./build/unillm_example
```

This example demonstrates SDK configuration and route wiring.
